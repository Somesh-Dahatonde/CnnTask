const nextConfig = {
  reactStrictMode: true,
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "g-mxtlc2zquep.vusercontent.net",
      },
    ],
  },
};

module.exports = nextConfig;
